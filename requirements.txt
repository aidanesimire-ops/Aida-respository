# Core (needed for fetch + tailor + tracker)
requests>=2.31
beautifulsoup4>=4.12
PyYAML>=6.0

# Browser automation (needed for `aida apply`)
playwright>=1.40
# After installing, run once:  playwright install chromium

# Optional: richer Claude-written cover letters when ANTHROPIC_API_KEY is set
anthropic>=0.40
